spring:
  datasource:
    url: jdbc:mysql://mysql:3306/eventdb?useSSL=false&allowPublicKeyRetrieval=true
    username: ${DB_USER:root}
    password: ${DB_PASSWORD:password}
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
server:
  port: 8080
